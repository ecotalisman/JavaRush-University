/*
Трим-уємо ім'я
Під час введення імен до БД хтось, здається, засинав на пробілі. Напиши запит, який із таблиці employee вибере імена (name)
співробітників: перша колонка — просто ім'я, друга — ім'я з видаленням пробілів з обох сторін,
третя — ім'я з видаленням пробілів з лівого боку та четверта — ім'я з видаленням пробілів з правого боку.

Requirements:
1. Запит має бути реалізований згідно з умовою.
*/
-- Write your code here:
SELECT name, TRIM(name), LTRIM(name), RTRIM(name) FROM employee;